<div class="card">
  <h1>CATEGORIES</h1>

  <ul>
    <% @categories.each do |category| %>
    <div class="card">
      <li><%= link_to category.title, category_path(category) %></li>
    </div>
    <% end %>
  </ul>
</div>
