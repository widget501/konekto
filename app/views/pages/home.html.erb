<div class="header-container">
    <h1>I-KONEKTO</h1>
  </div>

  <div class="cards-container">
    <div class="card">
      <ul>
        <% @categories.each do |category| %>
          <li><%= link_to category.title, category_path(category) %></li>
        <% end %>
      </ul>
    </div>
      <div class="card" id="random-posts">
  </div>
</div>
